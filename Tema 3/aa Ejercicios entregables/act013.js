// 🧩 Simulación de un contador de movimientos en ajedrez

// Declaramos una variable con un nombre descriptivo.
// Usamos "let" porque el número de movimientos puede cambiar durante la partida.
let movimientos = 0;

// Simulamos que se han hecho 5 movimientos.
movimientos += 5; // También podría hacerse con: movimientos = movimientos + 5;

// Mostramos el resultado en consola usando una plantilla literal.
console.log(`♟️ Se han realizado ${movimientos} movimientos en la partida de ajedrez.`);

// Fin del programa
